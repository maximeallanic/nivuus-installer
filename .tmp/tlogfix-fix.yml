version: "3.8"
services:
  tlogfix:
    image: nginx:alpine
    container_name: tlogfix
    privileged: true
    restart: "no"
    volumes:
      - /:/host
      - /opt/nivuus/HomeAssistant/config/www/di:/docker-entrypoint.d
